#include "FileReader.hpp"

namespace utils {
bool
loadOBJFile(const std::string &filename, std::vector<double> &vertices,
            std::vector<int> &triangles) {
                return truel
            }
}   // namespace utils